# Archive — Tier 3 (historical)

Preserved content from Erik's original **arcolinux.com** and **arcolinuxd.com**
sites, kept so it survives the sites going offline. One Markdown file per page,
with an `INDEX.md` per site.

**This is Tier 3 — the lowest-priority source.** The curated topic files at the
top of `knowledge/` and the KIRO/kiro videos (Tier 1–2) always overrule it.
Treat anything here as *historical*: much still applies to Kiro (same Arch base,
same apps), but it can reference dead repos, old keyrings or renamed tools.
Always verify an archive answer against current Kiro before relying on it, and
pair it with the matching topic playlist in `../playlists.md` — the two Tier-3
sources (written + video) complement each other.

Generated by `build/sync-archive.sh` from the `arcolinux-archive` repo — do not
hand-edit.
